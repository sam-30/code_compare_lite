services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./output:/output
    environment:
      OUTPUT_DIR: /output

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
