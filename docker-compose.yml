services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./output:/output
      - /Users/sam:/Users/sam:ro
    environment:
      OUTPUT_DIR: /output

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
